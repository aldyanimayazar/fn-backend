export enum Days {
    DAYS = 'DAYS',
    WEEKS = 'WEEKS',
    MONTH = 'MONTH',
}

export enum UnitWeight {
    KG = 'KG',
    MG = 'MG',
}

export enum UnitMeasurement {
    M = 'M',
    CM = 'CM',
}